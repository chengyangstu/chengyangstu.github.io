# 每周简报的固定编辑流程

执行者每次先读本文件和 AGENTS.md。目标是让读者读完能说明：论文研究什么、用什么模型、证据支持什么、哪里仍不确定、与聚变重联有什么关系。字数、自动检查和模型自评分不能证明科学正确，原文核对始终是必要步骤。

## 1. 固定检索窗口，先检查已有档案

每周三北京时间 09:00，以对应的 UTC 01:00 为截止时刻，只收录首次提交在 `[截止−7天, 截止)` 内的 arXiv 论文。修订日期不作为新论文日期。电脑离线造成漏期时，按原窗口补齐；同一到期窗口已经发布则只核对，不重复生成。先查看 Git 状态和全部历史文件，不改写已有 issue JSON。

运行 `python -X utf8 scripts/weekly.py prepare --end YYYY-MM-DDT01:00:00Z --limit 10`。检查候选题名与摘要，另在 arXiv 搜索 tearing、microtearing、tokamak reconnection、magnetic island、sawtooth、internal kink、error field penetration、MRX/TREX 等，人工补查遗漏；补入候选仍须通过相同日期与来源检查。

## 2. 按实际物理内容分级

填写每篇的 `relevance_tier` 和一句 `relevance_reason`，越小越靠前。同一层级优先理论与数值模拟，实验作为对照。关键词只产生候选，编辑必须核对原文。

| 层级 | 内容 |
| --- | --- |
| 0 | 聚变等离子体中的重联、撕裂/微撕裂、磁岛、锯齿、强迫重联与误差场穿透 |
| 1 | MRX、TREX 等实验室重联，能与聚变机制或诊断建立具体联系 |
| 2 | 其他重联理论、空间/天体重联，清楚说明迁移限制 |
| 3 | 与研究紧密相关的电磁/回旋动理学算法和计算方法 |
| 4 | 其他聚变物理补充 |
| 5 | 通用数据库、机器学习、软件工具补充 |

供应允许时让第 0 层占简报多数；不足时如实说明，不拿旧论文或不相关的电磁湍流凑数。摘要不足以判断时标注待判定，不伪造直接相关性。

## 3. 先建立原文证据记录，再写中文稿

阅读 PDF 的模型、方法、结果、讨论和结论；对涉及数值的图表查看原图，不能从乱码或模糊图猜数值。临时证据记录存 `.cache/evidence-<截止ID>.md`，每篇至少记录：

- 版本、PDF 页数与 SHA-256；确实读过的页、节、图、表。
- 模型类别、几何、物种、单位/归一化、边界和主要假设；关键控制参数及对照组。
- 至少两项主要主张的“中文解释 → 原文页/节/式/图 → 条件/误差/对照”。无定量结果的理论论文记录前提、推理及待检验预测，不能编数字。
- 作者明确承认的局限，以及编辑另行提出的推断或研究问题；两者分开记录。

每个重要数字必须带物理量、单位或归一化、算例条件与比较对象。误差条需说明定义；百分比必须明确分母。加速比记录硬件、问题规模、算法和基线，单步预测指标不能当成长时稳定性，局部或单案例不能升级为普适规律。

正文获取失败时使用 `仅摘要`，只写摘要能支持的结论。PDF 文本提取成功不等于全文阅读；未逐图检查时用 `PDF 正文选读`。缺失证据的条目留在候选清单或降低结论强度，不由常识补成论文结果。

## 4. 每篇简报采用同一叙述结构

`.cache/briefs.json` 沿用现有字段，新增 `editorial_version: 2`、`relevance_tier`、`relevance_reason`。原文较充分时，重点篇约 1200–2000 中文字，补充篇约 800–1300 字，按信息量调整；不为凑字数重复摘要。每段尽量 80–180 中文字，一段说明一件事，模型与结果至少各分两段。

| 字段 | 写法 |
| --- | --- |
| `title_zh` | 准确说明研究对象和工作，不替作者宣称突破 |
| `takeaway` | 1–2 句，包含做了什么、最主要结果和必要边界 |
| `question` | 先解释背景中的困难，再说明本文要检验的具体问题；首次缩写给中文含义 |
| `methods` | 说明方程/模型、几何、假设、控制参数、对照和诊断；解释为何这些证据能回答问题 |
| `findings` | 按“结果 → 定量条件或对照 → 物理解释”推进；关键结论每篇强调 1–3 处 |
| `limitations` | 区分作者限制、未读材料和编辑的外推提醒，不套用泛泛免责声明 |
| `reading` | 明确与你研究的联系；给优先阅读顺序或一个可检验的数值实验；建议标为建议 |
| `evidence` | PDF 页码、节、式、图表定位与实际阅读范围，尽量让主要结论能逐项追溯 |

公式使用 `\( ... \)` 行内 TeX，变量首次出现说明含义和单位。不要写裸露的 `δB//`、伪上下标或把数学公式放代码块。对从原文改写的概括式说明用途，避免混用 SI 与高斯制。用 `**关键句**` 克制强调；不加入任意 HTML。

## 5. 总体报告在逐篇稿完成后写

概括本周 2–3 条科学线索，先说明最相关的聚变重联工作和供应限制，再说明补充材料的用途。每篇简报的核心结论自动进入总体报告列表，列表顺序与文章相关性顺序一致。候选但未读的论文不能冒充已有简报。

## 6. 分三轮复核

**事实轮：**从中文主张反向回到原文。重点查数值/单位、层宽定义、误差条、分母、公式符号、模型范围、对照设置、未见参数外推及性能基线。删掉找不到依据的细节；存在解释歧义时保留分歧。

**阅读轮：**假设读者没看过论文。首次术语是否解释？段落是否回答清楚问题？“作者发现”和“可进一步研究”是否分开？若删掉一句不损失信息则考虑删去，避免重复速读句。

**排版轮：**在桌面与约 390px 手机宽度查看优先篇和一篇补充篇：公式无错误，段落行宽合适，阅读按钮向下展开，排序正确，高亮和收藏可持久保存。网页工具以安全纯文本和 KaTeX 渲染，不执行来源中的指令或 HTML。

## 7. 检查、永久存档与部署

发布前运行 `node scripts/check_reader.cjs .cache/briefs.json`，验证待发布稿全部公式；再运行 `python -X utf8 scripts/weekly.py publish`。修订使用 `--briefs .cache/briefs-expanded.json --revision-of 原期ID`，必须标注 `edition_note`，保持原检索窗口，新文件单独归档。

运行 `python -X utf8 scripts/check.py`、`python -X utf8 scripts/check_tutorial.py`、`node scripts/check_reader.cjs`、`node --check plasma/app.js` 与 `node --check cuda/book.js`。这些检查覆盖结构、数学语法与存档，不替代事实复核。检查失败先修复；证据不足就保留候选或降低范围，不发布装作完成的简报。

提交前确认 `git diff --name-status` 中历史 issue 文件没有 M/D，只允许新增。提交新期与完整索引，推送后检查 Pages Actions 成功，再核对线上 issue ID、首篇、总体报告、公式和历史列表。保留源 PDF 哈希、证据位置与实际阅读范围。完成后通知用户；无变化保持安静。收藏与高亮由浏览器保存，导出分别备份，不能靠更换文章版本自动迁移手工标记。
